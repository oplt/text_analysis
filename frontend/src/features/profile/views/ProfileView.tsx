import { Box, Skeleton, Stack } from "@mui/material"; import { SettingsTabs } from "../../../components/layout/SettingsTabs"; import { PageShell } from "../../../components/ui/PageShell"; import { QueryErrorAlert } from "../../../components/ui/QueryBoundary"; import { AccountSection, MfaSection, PasswordSection, ProfileSummary, PublicProfileSection, SessionsSection } from "../components/ProfileSections"; import { useProfileView } from "../hooks/useProfileView";
export default function ProfilePage() { const m = useProfileView(); if (m.userQuery.isLoading || m.profileQuery.isLoading) return <PageShell maxWidth="xl"><SettingsTabs /><Skeleton variant="rounded" height={420} /></PageShell>; if (m.userQuery.isError || m.profileQuery.isError) return <PageShell maxWidth="xl"><SettingsTabs /><Stack spacing={2}>{m.userQuery.isError && <QueryErrorAlert error={m.userQuery.error} fallback="Failed to load account details." onRetry={() => void m.userQuery.refetch()} />}{m.profileQuery.isError && <QueryErrorAlert error={m.profileQuery.error} fallback="Failed to load profile details." onRetry={() => void m.profileQuery.refetch()} />}</Stack></PageShell>; return <PageShell maxWidth="xl"><SettingsTabs /><Box sx={{ display: "grid", gap: 2, gridTemplateColumns: { xs: "1fr", lg: "minmax(320px,.92fr) minmax(0,1.08fr)" }, alignItems: "start" }}><ProfileSummary m={m} /><AccountSection m={m} /><MfaSection m={m} /></Box><Box sx={{ display: "grid", gap: 2, gridTemplateColumns: { xs: "1fr", lg: "minmax(0,1.05fr) minmax(320px,.95fr)" } }}><PublicProfileSection m={m} /><Stack spacing={2}><SessionsSection m={m} /><PasswordSection m={m} /></Stack></Box></PageShell>; }
