# .understandignore — patterns for files/dirs to exclude from analysis
# Syntax: same as .gitignore. Built-in exclusions also cover node_modules,
# virtual environments, build outputs, lockfiles, binaries, and logs.
#
# Uncomment a pattern only when that area is outside the graph's intended scope.

# docs/
# backend/tests/
# backend/scripts/
# *.test.*
# *.spec.*
